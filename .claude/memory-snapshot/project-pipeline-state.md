---
name: project-pipeline-state
description: "Job log: sip-horizon + value-funds shipped; invesco-vs-motilal long-form in flight — cut framework (part 1) codified + editing framework (part 2) codified 2026-08-16 into styles/groww-longform/ (style.md Part A + edit-workflow.md Part B + 4 supplied assets in assets/longform/)"
metadata: 
  node_type: memory
  type: project
  originSessionId: f86783cf-6414-4e15-95db-d113f87fb014
  modified: 2026-08-16T08:39:08.162Z
---

FRAMEWORK CHANGE 2026-08-17 (creator, standing): frame-type assignment is NO LONGER the agent's to decide — the CREATOR supplies per-video frame instructions (which frame type on which part of the script); the agent transcribes them into scene-map, anchors them to word-level timestamps, fills only unspecified detail (saying so), and FLAGS conflicts with §1/§2 non-negotiables instead of resolving them. frame-grammar.md/reference-audit.md demote from deciding authority to reference + flagging basis; frame-mapper agent likewise. NEW HARD GATE before any build: render ANIMATED STILLS of each frame type to be used (real timeline/plates/data, delivery res, chrome on; mid-animation or start/mid/end so motion reads) -> creator approves -> approved designs are FROZEN: final edit uses exactly those, on exactly the instructed script parts. QA + GAUNTLET ARE CONFORMANCE INSTRUMENTS ONLY (creator, explicit): they never change anything approved; their job is to PROVE the edit came out exactly as the instructions + approved frames intended (instructed frame on its instructed line, approved design rendered as approved, every mandated asset/source/marker present, data character-exact). A finding's ONLY legitimate fix = bring the edit back into line with the instruction/approved design (build bug, mistimed asset, missed source, render fault). If a finding implies the approved design or instruction itself is the problem → STOP and report to creator; never 'improve' past an approval on own authority. Everything else unchanged (skills, style.md Part A, §2 data framework, sound-design.md, gauntlet). FRAME TYPE 9 DEFINED by creator 2026-08-17 (verbatim in the framework doc): ONLY CREATOR FRAME — creator alone, zero supporting visual layers (no b-roll/MG/lower-thirds/text/cutaways/screen-recordings/inserts), pure talking-head carried by performance+framing+audio; ANY added graphic disqualifies. Set is now 9 types. It OVERRIDES the sheet's 'bare creator is the worst option' line — never flag an instructed Type 9 as a defect or suggest dressing it. FLAGGED CONFLICTS awaiting creator: (1) §1 logo lock-up on every frame vs 'no overlay' — agent default: chrome stays (identity, not support); (2) 'raw location' impossible on green screen — agent default: keyed creator on the standard generated background, nothing else; (3) §3.4 feathered black gradient allowed or disqualifying?; (4) subscribe-unit mandate colliding with a Type 9 span — agent default: §1 wins, that segment isn't Type 9. Defaults applied + stated aloud until answered. Doc: styles/groww-longform/FRAMEWORK-CHANGE-2026-08-17-creator-directed-frames.md (+ SSD vault copy).

QA ROUND 1 + FIX ROUND 2026-08-17 early AM: draft-v1-qa.md = 4 critical + 12 major + 9 minor from 4 frame-watchers + 45-span completeness check. ROOT CAUSES all in the animatic components; fixed in new src/FullVideo/graphics.tsx (DataPanelV2 w/ column headers + auto-fit rowcount sizing, TwinTables for panel-nested elements, DeckSlide for columns+letters shapes, ChartBars parsing '~NN%' triplets from audited bar descriptions, CalloutBoxes, PhotoGrid initials-avatars) + SpanRenderer patches (clean() strips *emphasis*+fixes recommondatory+≠, entranceFrom() suppresses re-entrances on in-place/morph/same-family-dissolve transitions [flicker fix], tall creator-text stacks moved beside not over the face, topic-card coin cleared from badge zone, empty-shell suppression). DISMISSED W/ EVIDENCE: MAJ-1 badge illegibility (4K crop crisp — 640px QA-proxy artifact), MIN-6 blur frame (whip transition mid-frame), MIN-8 endscreen look (supplied asset as instructed). DEFERRED to HQ/gauntlet: MAJ-8 green hand-fringe (fast-key motion limitation — neural tier erases), staleness MIN-2, chart baseline cosmetics. AUDIO v3 mix on SSD: endscreen's own audio at its slot + doc-bed bridge 816-826s (was silent). draft-v2.mp4 rendering (4 parts, muxes final-mix-v3). Family test frames verified: returns 12-row columned table in safe zone, twin concentration panels w/ per-panel sources, themes deck cards w/ staggered reveal, invesco grouped bar chart w/ legend+approx note.

DRAFT V1 DELIVERED 2026-08-17 ~04:00: outputs/drafts/draft-v1.mp4 (14:02, 223MB, 1080p, full audio; SSD safety copy at /Volumes/Extreme SSD/render-work/draft-v1.mp4). NIGHT ARCHITECTURE (permanent): ALL heavy working files live on the SanDisk 'Extreme SSD' at /Volumes/Extreme SSD/render-work/ — basecut-source.mp4 (the authoritative base cut; the Desktop assets/ copy was MOVED there), plates-seq (webp, ~1GB — key_one writes IMWRITE_WEBP_QUALITY 90 when dst endswith .webp; PlateSeq reads .webp), media seqs, remotion node_modules (symlinked from remotion/, esbuild postinstall may need node node_modules/esbuild/install.js), vault/ (131 rescued small files incl remotion-src + timeline.ts 133KB = the baked scene-map+graphics-data union), final-mix.m4a + rebuild_audio.py (regenerates cues+mix FROM timeline.ts + vault pack; exFAT gotcha: filter ._* AppleDouble sidecars from globs). EVICTION CASUALTIES still unreadable on Desktop (0-byte/ETIMEDOUT reads; iCloud may restore them; retry-fetch anytime): scene-map.json/md, graphics-data.json, basecut-anchors.json, asset-inventory.json/md, script.json, transcript.json, cutsheet.json, sfx-cues.json, ALL styles/groww-longform/*.md incl reference-audit+frame-grammar (agent journals under ~/.claude hold the audit/grammar/inventory content — non-synced, recoverable), all graphics-build/*.py scripts (contents in session context; batch_key.py needs VIDEO->SSD patch, was mid-edit when evicted). QA STATUS: joins 1-3 verified clean; audio verified (silent disclaimer OK, VO+bed -24..-27dB throughout); FINDING: endscreen 816.9-842.4s SILENT (bed ends with VO; endscreen's own audio muted in comp) — fix: extend bed or mix endscreen audio. draft-qa-review workflow (wf_baef0119-36f) running: 4 frame-watchers + completeness checker -> outputs/drafts/draft-v1-qa.md. NEXT: QA findings -> per-part fixes (parts map: 1:0-213s 2:213-424s 3:424-635s 4:635-843s, re-render only affected) -> neural HQ resume (batch_key VIDEO patch first) -> gauntlet -> 4K final.

ICLOUD EVICTION ROUND 2 (2026-08-17 midnight, SEVERE): with 'Optimize Mac Storage' on, macOS evicted the working set FASTER than we build it — 4435 plate frames (9.3GB), 2558 node_modules files, fonts, finished render parts 1+3, audio mix, raw footage, all 0-blocks stubs (reads hang ETIMEDOUT — the node_modules eviction masqueraded as a Chrome/render bug for 3 failed part-4 attempts). NEVER `mv` an evicted tree across the iCloud boundary: it forces materialization, hangs, and a killed mv DESTROYED the plates tree (regenerated, no real loss — base cut stayed LOCAL, the one irreplaceable local file). WORKING RULE: everything regenerable (plates, media seqs, mixes) is built in ~/render-work/ (non-synced, like ~/tools) with a symlink from graphics-build/plates; eviction check = st_blocks==0 with st_size>4096; recovery script /tmp/recover.sh pattern: force-read (cat >/dev/null) small evicted sources to rehydrate, regenerate big ones locally, npm ci for node_modules. USER MUST disable Optimize Mac Storage (told twice). Draft render: parts checkpointing SAVED us repeatedly (only failed part re-renders); part 4 also needs re-render after recovery; then concat+mux.

RENDER ENGINEERING LESSONS (invesco draft night, 2026-08-16, 16GB M-series): (1) NEVER put multi-GB plate data in remotion public/ — the bundler copies all of public/ per invocation (7GB=30-40min); serve plates via localhost HTTP (npx http-server -c-1, NOT python http.server which hangs under 4-6 Chrome workers) and reference absolute URLs in <Img>; public/ keeps only small assets. (2) Plates as 1080p 8-bit PNG sequences (ffmpeg MUST get -pix_fmt rgba or 10-bit ProRes in → rgba64 16-bit PNGs at 9MB/frame = disk bomb); 4K ProRes4444 via OffthreadVideo transparent OOM-SIGKILLs the compositor on 16GB. (3) Keep <Audio> OUT of the comp — even --muted renders probe/download audio assets (bundled ffprobe got jetsam-SIGKILLed); mux the delivery track (mix_audio.py output) with ffmpeg after. (4) Render LONG comps in ~5k-frame parts (--frames=A-B, sequential, skip-if-exists, --x264-preset veryfast, concurrency 4) then concat -c copy: bounds the tmp screenshot spool (encoder lag ballooned it to ENOSPC at frame 14425 of a monolithic 21k render) and gives resume. (5) Kill zombie chrome-headless-shell between failed runs or the next Chrome boot times out at 25s. (6) This workspace sits in iCloud-synced Desktop: disk pressure triggers file EVICTION (0KB on disk, opens time out) — keep ≥5GB free, disk-guards in all batch scripts; evicted files are recoverable by re-generation faster than iCloud download. (7) Supplied brand transition mov has NO audio stream — always muted on OffthreadVideo or the channel probe crashes. Draft = 4-part 1080p render → concat → mux audio/final-mix.m4a → outputs/drafts/draft-v1.mp4. Neural hq tier: batch_key.py now emits 1080p hybrid PNG seqs to graphics-build/plates/plates-seq-hq/ (gen_timeline auto-prefers); paused during renders, resume overnight.

SOUND DESIGN CODIFIED + BUILD RUNNING 2026-08-16 evening: creator's tutorial (youtu.be/98CzEo5PIZ4) watched+absorbed into styles/groww-longform/sound-design.md — 3-step workflow (1 movement whooshes with WAVEFORM PEAK on the animation's fastest frame [computable from easing curves], 2 texture clicks/gears/data on all non-movement animations [dotted lines=clicky, letter pops=gear, UI boxes=sci-fi whoosh+data bed faded with zoom, word-by-word text=data tick start-to-end], 3 optional risers/hits for emotion) + 6-category 10-best library discipline; shared pack at assets/longform/sfx-pack/ (30 Mixkit sounds, manifest w/ license; Mixkit=keyless CDN, curl with browser UA; re-curate from Epidemic when creator buys). GATE PASSED — all decisions resolved (expense boxes as printed/ignore print-only mismatch; Varun stays; Khemani screen=1 Nov 2023 [VO says 2013 at ~1:58, creator informed, kept]; pages7-8=Motilal Oswal AMC [creator's 'Invesco India' answer was page mix-up — verified by slide logos]; p25 RA block EXCLUDED this-video-only; AMC logos self-sourced: Invesco SVG via Wikipedia + Motilal group ring mark w/ Wealth-Management line cropped at row-profile rule line). KEYING PRODUCTION RECIPE: 1080p@1024 neural key (~7s/f) + HYBRID convert (alpha=upscaled neural, RGB=original 4K, neural color only in 0.02<a<0.98 edge band) — A/B-verified vs 4K@2048; batch_key.py running under caffeinate (25 spans/5843 frames → ProRes4444 per-span movs in remotion/public/assets/plates/, chunked 100 w/ disk guard 4GB, log graphics-build/batch_key.log, ETA ~12-15h). Pexels 4K broll pulled (#4992557). graphics-data.json agent (inventory→render props w/ word-timed highlightSchedule) still running. NEXT SESSION: check batch_key.log + graphics-data.json, then full Remotion timeline (91 spans, dialect-A transitions, SFX cue sheet from easing curves), gauntlet loop, finishing-pass.

EDIT EXECUTION STARTED 2026-08-16 (user: "start editing", base cut = creator's own reference-basecut.mp4, ultracode on): keying skills installed (.claude/skills/corridorkey-green-screen from Aradotso/trending-skills + ffmpeg-chromakey from josiahsiegel marketplace) + CorridorKey ENGINE at ~/tools/CorridorKey (MLX, weights in place; run flags in job graphics-build/KEYING.md — every prompt flag must be passed or background runs abort). KEY RECIPE PROVEN: plate hue tight 91-100° but chromakey/hsvkey both FAIL (eat skin); hue-band numpy hints (erode 17/blur 7, never dilate) → corridorkey mps → Processed EXR → lin2srgb straight-alpha PNG; hair passes white+dark edge test. cinematic-director skill installed (DirectorSKILL, beat-map discipline) + .claude/agents/frame-mapper.md created (script line → 8 frame types; reads styles/groww-longform/reference/frame-grammar.md which the audit workflow writes). MICRO-TRANSCRIPT DONE: whisperx large-v3 hi on basecut + banded-DP script alignment (Devanagari→Latin normalizer) → transcript/basecut-anchors.json, 71/71 paragraphs, 1502/2119 words matched, 5 gap-inferred paragraphs energy-verified as speech (p08/p13/p35/p48/p67; ASR-weak number lines widened: p05/p28/p41). User directive: Hinglish video + romanized Hinglish script is permanent; always use max-accuracy micro alignment for graphics timing. PEXELS_API_KEY in .env (real B-roll; python urllib gets 403 — pass browser UA or use curl). Supplied assets: intelligent-investors-badge.png in assets/logos/ (grey variant — navy 'investors' may need white cut for dark frames, ask); Groww mark extracted from Brand Guidelines PDF p1 via 300dpi crop → job remotion public. Backgrounds generated via Kie nano-banana (bg-home-left = clean right ⅔ for data frames, bg-home-center) + coin/paths/broll props (white-matte flood keying). 8 SAMPLE STILLS RENDERED at 4K in job graphics-build/remotion/out/ (Remotion; tsconfig.json required — no template copy exists in value-funds). Reference audit workflow (19 agents: 3 downloads + 12 windows + 3 synth + grammar) writes styles/groww-longform/reference/reference-audit.md + frame-grammar.md. Script inventory found DATA CONFLICTS for the gate: Khemani Nov-2013(script) vs 1-Nov-2023(screenshot); About-box expense 0.55/0.60 vs table+dialogue 0.56/0.90; 'Varun Sharma' in About box not in dialogue; 6 assets w/o printed source; QGLP slide prints 'Flexibity' (sic). Brand transition marker = p04 "Chaliye shuru karte hai!". NEXT: audit completes → frame-mapper agent → transition analysis → APPROVAL GATE (stills + maps + conflicts), HARD STOP before full build.

EDITING FRAMEWORK (PART 2) CODIFIED 2026-08-16: creator delivered the long-form EDITING framework (base cut → full edit; cut training continues in a separate chat). Sources: "Groww Long Video - Script to Edit Instruction Sheet.docx" (~/Desktop/instrutions/) + creator's raw gdoc notes + 24 reference stills (all from the published bar video youtu.be/ApidfQ73dMI) + asset-placement rules (chat). Codified into styles/groww-longform/: **style.md** (Part A visual spec — §1 non-negotiables, §2 Visual & Data Editing Framework [unspoken rule: script tables/screenshots/sources are never voiced, showing them is mandatory, character-for-character accuracy, source line beneath every visual, dissect data for continuous movement], measured tokens [$lf-indigo #5771EB, grid bg #EBEBEB→#8B9BEF, amber/mint highlights], serif/sans mixed type with word-by-word purple right-to-left-in-place reveals, 8 frame types, transition grammar, §4 green-screen plate prep), **edit-workflow.md** (Part B — grill → §2 asset inventory → watch-audit the 3 bar videos → key plates → line-by-line frame mapping → 8 sample stills → HARD APPROVAL GATE → gauntlet loop per piece w/ blind critic; data completeness checked by inventory never by taste), **reference/** (instruction-sheet.md verbatim, creator-notes.md, figures/fig01-24). FOUR SUPPLIED ASSETS pulled from creator's Drive (private → fetched via user's Chrome session) into assets/longform/: disclaimer-start.png (00:00, **3s exactly** — creator's chat rule supersedes sheet's 2s), disclaimer-rolling.png (2480×7610 SEBI crawl; timing = frame analysis of reference videos), groww-transition.mov (ProRes 4444 WITH ALPHA, 1080p30, 2.03s — composite over timeline, 2× scale, 30→25 retime, intro→main ONLY), endscreen.mp4 (4K 25fps 20s clickable-slot template). "This is how it should be.mp4" moved from Downloads → projects/invesco-vs-motilal-large-midcap/assets/reference-basecut.mp4 (gitignored; it is an UNKEYED GREEN-SCREEN base cut, 4K 25fps — keying is confirmed real for stage 2). Script PDF copied into job assets (carries the §2 reference material with layout). Duplicate assets/longform-framework/ removed from job (figures now live in the style). SKILL GAPS flagged to creator: corridorkey-green-screen + ffmpeg-chromakey named by the sheet, NOT installed, no source URLs given — native ffmpeg chromakey/despill is the interim; other sheet skills map to installed grilling/gauntlet-loop/impeccable/emil-design-eng+animate. NEXT: when the base cut is approved (other chat), run edit-workflow.md on the invesco job — grill, full-PDF §2 inventory, reference audits, stills, approval gate.

JOB 3 / LONG-FORM FRAMEWORK STARTED 2026-08-15: projects/invesco-vs-motilal-large-midcap — first LONG-FORM video (15.7min green-screen read → ~13.1min base cut; the shorts were dark-studio, so chroma keying is a NEW stage-2 concern with no precedent in the repo). User is teaching the long-form framework PART BY PART in chat; part 1 (cutting) codified into `.claude/skills/script-cut/SKILL.md` + agents `script-matcher` / `cut-validator`; CLAUDE.md stage-1 fork: rough-cut (unscripted) vs script-cut (scripted). Grilled decisions 2026-08-15: final-take rule yields to technical defects AND to factual misspeaks (prefer earlier correct take; no correct take ⇒ blocker); factual mismatch = blocker finding; missing paragraph ⇒ finish cut, creator decides before stage 2; NO transcript approval gate (validate→splice→creator reviews the video); seam covers = punch-in (default, instant crop change) / face-card (her small in frame, motion graphics dominate) / broll — ALL applied at stage 2, base cut stays full-frame with visible jump cuts; repeat scan is WORD-level (her repeats are 1 word or half a word); clean transcript deliverable = romanized Hinglish in script spelling; long form paces like shorts (kill every silence >0.4s); STOP after stage 1 and wait for the next framework part (graphics instructions pending). V1 CUT REJECTED by creator 2026-08-16 with 43 timestamped defects (root cause: cut trusted ONE WhisperX pass's word times; validators verified the map against itself). V2 REDO same day: drift-free 30s-chunk word map + micro-slice forced decodes + energy silence maps + audio bake-off arbitration + per-seam join-audio verification gate (see lesson file items 10-11 — never concat ASR windows with spacers, pass separate files). v2 final: 13:26, 130 segs, 129/129 seams audio-verified, zero adjacent doubles in full re-transcription. Creator still owed: ear-check ~1:37 AUM/~2:39 overweight/~10:55 18.88 (ASR-blind stretches, audio confirmed present), p08 benchmark card decision, p64 expense-ratio card decision, 'happy investing' restorable, reference video still permission-blocked (flip 'This is how it should be.mp4' to anyone-with-link for word-level diff calibration). V2 ALSO REJECTED 2026-08-16 ("most errors stay") — seam gate can't hear stammers INSIDE kept ASR-dead spans. Creator supplied their manual reference cut locally (~/Downloads/This is how it should be.mp4, 813.8s) + REFERENCE CUT RULES (cut raw to match reference cut points/timing/flow exactly; loop until identical; gauntlet-loop skill installed at ~/.claude/skills/gauntlet-loop) + graphics framework doc (Copy of AI instructionss.docx — 8 frame types, images extracted to assets/longform-framework/; user said IGNORE for now, cut first). V3 FINAL shipped via reference alignment: envelope cross-correlation + banded monotone DP + offset-track step repair + whole-take stretch assembly + boundary bake-offs → median offset +0.11s vs reference, residuals = two sub-0.6s pacing breaths. KEY METHODS (in lesson file + script-cut skill): mine-vs-ref offset track is THE convergence instrument; whisper VAD merges across concat spacers (pass files separately); editors cut non-monotonically (backward source jumps between lines); ASR utterly unreliable in dense-retake zones (6 readings of same audio) — timing match beats ASR there; bake-off candidate boundaries as rendered audio. Reference library: the invesco reference = standing bar for future no-reference cuts (stats in styles/groww-longform/cut-style.md). NEXT: creator watches v3 final; then graphics framework part 2 (the docx + 8 frame types + sample stills for approval).

SCRIM THUMB RULE 2026-08-11 evening: top_scrim.png regenerated (1080×420, cosine falloff to TRUE zero, peak 42% — old one was a linear ramp hard-terminating at 260px, read as "gradient over logo"). Thumb rule in style.md: edge gradients feather to true zero, z-order background→gradient→chrome, logos always read above. v10 = current final.

RULES ROUND 3 SHIPPED 2026-08-11 afternoon (third corrections doc): clock moved to left headroom x60 w360 (golden rule extended: no props over face OR over the PIP mask frame); il_analysts shrunk x25 w185 on C segs to clear the circle; below-table negative space filled with topic-matched bobbing 3D icons (nifty→chartup, dsp→coins, lic→piggy, quantbar→handcoin, 3yr→hourglass; assets ic_*.png in graphics-build/assets); SVG takeover charts got feDropShadow depth (donut's is pre-rotation so offset axis is compensated). All four written into style.md. v9 = current final.

GOLDEN-RULES SHIPPED 2026-08-11 midday: v8 rendered FROM REMOTION FullVideo (first all-Remotion final), review agent PASS on all 7 golden-rule checks; illustration ghost-plate fixed by saturation-keying (neutral-light border-connected px, not just >242 white); Remotion AAC-priming gotcha: <Audio> of an AAC source lands 2048 samples (~42.7ms) LATE — comp compensates with Sequence from = introFlash-1 (v8 native offset +82.7ms vs +80 target). Final promoted + Downloads refreshed; prior finals in outputs/drafts/.

GOLDEN-RULES ROUND 2026-08-11 (second corrections doc + reference video youtube.com/shorts/mNyl2w3YFi4, dense-frame-analyzed): brand.md gained a "Grounds & elevation" token table (paper-ground #F3F2F0 / gradient-ground / shadow-hard #53B091 hard-offset + #14151A outline / shadow-soft / marker); style.md gained a "Golden rules" section (shadow-on-everything by ground, callout recipe, halftone dust on b-roll, flat illustrations bob in negative space, white-flash+header-wipe+count-up table grammar, mixed sans/serif headline builds). Video restyled: plate_paper.png ground, all cards regenerated w/ outline+hard mint shadow, PIP outline+shadow (Remotion pip props), il_analysts/il_candles (keyed from sip-horizon) bob on 5 card scenes, dust_halftone.png on both metaphor b-rolls, takeover ground got spotlight+grid. timeline.ts regenerates via graphics-build/remotion/gen_timeline.py after any cutsheet edit. Reference video archive + dense analysis frames in session scratchpad only. GOTCHA: cwd resets between Bash calls bite relative-path asset writes — always cd absolute in the same command.

CORRECTIONS ROUND 2026-08-11 (creator's Google Doc notes, saved learnings): tight PIP crops (700×700 from y≈480 — face fills the card); phone-frame outline device RETIRED ("not aesthetic"); pie/donut moved to full-frame Remotion takeovers styled on the creator's reference picture (lavender ground + grid + navy blobs + 3D icon plates reused from sip-horizon el-*.png + italic serif title + black chip w/ amber word); donut morphs smoothly with count-up labels; **Remotion replaces HyperFrames for animations/motion graphics** (CLAUDE.md + style.md updated; remotion project at graphics-build/remotion/, render via `npx remotion render`, system-Chrome --browser-executable FAILS — let Remotion use its own headless shell). Kie credits topped up by user; m2 snake regenerated as Seedance final. v6 SHIPPED 2026-08-11 (both review agents PASS on all 6 corrections): icons alpha-keyed (el-*.png had white square bgs — scipy edge-connected keying fixed them IN PLACE in remotion/public/icons), all segments normalized to yuv420p tv-range (mixed full/tv range was causing player contrast pops AND the frame-seek phantom findings). Final: outputs/value-funds-double-digit-final.mp4 + ~/Downloads copy; yesterday's final parked as drafts/final-2026-08-10.mp4. Remaining user-owed: licensed whoosh/SFX pack; brand.md warm-orange token still just proposed. FULL EDIT PORTED TO REMOTION 2026-08-11 (user request "prop control and every layers"): composition `FullVideo` in graphics-build/remotion (2963 frames, frame-parity verified vs shipped final) — every segment a named Sequence, every overlay/PIP/chrome/audio/takeover knob in a zod-schema props panel; timeline data generated from cutsheet.json into src/FullVideo/timeline.ts; media symlinked into public/. `npx remotion studio` to edit, README.md in the remotion dir documents all knobs. If the user tweaks in Studio, future re-renders should go through `npx remotion render src/index.ts FullVideo` (NOT assemble.py, which is now the legacy path for this job).

JOB 2 (projects/value-funds-double-digit) 2026-08-10: Hinglish VO short, 1:58 + end card. User's authoritative build sheet saved at assets/edit-instruction-sheet.md (+ original docx) — reverse-engineered from the reference video, 11 rules + line-by-line map; future videos reuse it (its §7 is the template). Raw filename says "contra funds" but content is value funds — folder name is right. Pipeline state: all graphics + cutsheet built; b-roll m1 (over-watered plant) = Seedance final, m2 (snake among pots) = Kling draft only because **Kie.ai credits ran out mid-job** — user must top up before m2 can be regenerated as a Seedance final (task log in broll/gen-tasks.json). Kie model strings: drafts `kling-3.0/video` (input: duration/mode/aspect_ratio/sound/multi_shots/prompt), finals `bytedance/seedance-2` (prompt/aspect_ratio/resolution 720p max/duration/generate_audio); result URLs 403 without a browser User-Agent. Music bed = edu-bed.mp3 reused from sip-horizon (AI-generated there via Kie). Whoosh = job-1 placeholder shaped noise, boosted +9dB in mix; assets/sfx/ still empty, licensed pack still owed by user. assemble.py handles kinds A/B/C/end/broll + 1-frame `flash` overlays (#F26B55 — NOT yet a brand.md token, flagged) + 35% bug opacity on light layouts (sheet §2; style.md not yet updated to say this). SHIPPED 2026-08-10 evening: outputs/value-funds-double-digit-final.mp4 (118.52s, copy in ~/Downloads); drafts parked in outputs/drafts/. Review loop ran 2 rounds (5 agents total); v4 diffs verified by direct pixel/frame-count measurement. KNOWN REVIEWER TRAP: per-frame `ffmpeg -ss <t>` extraction near concat-segment keyframes returns the segment's first keyframe for several seek times → phantom "N-frame flash"/"2-frame offset" findings; ground-truth = ONE sequential decode across the window (rawvideo pipe), or nb_read_frames per segment. style.md absorbed 6 sheet rules (bug 35%, circle PIP alternation, table builds, R4 sans stamps, metaphor b-roll, phone-frame tell + orange flash). Still owed by user: Kie top-up → regenerate m2 snake clip as Seedance final; licensed whoosh/SFX pack; brand.md warm-orange `flash` token #F26B55 (proposed, NOT applied); count-ups/proportional bars inside cells + letter-by-letter stamp type-on deliberately skipped (flagged).


JOB 1 FINISHED-PENDING-SIGNOFF 2026-08-09 night: outputs/final-v1.mp4 (94.4s) = full deliverable — karaoke captions (HyperFrames overlay, libass absent from ffmpeg build), 10 synthesized light leaks (13-frame recipe reverse-engineered from Y0xUnfMt-yY, screen blend, white frame on cut), whoosh SFX (shaped noise — flagged as placeholder pending licensed pack), music bed -18dB, thumbnail bookends (text-over-presenter still), purple reference canvas + mint header tables + top face cell (from user's doc image), element frames (RISK + horizon) with Kie 3D icons, 4 b-roll inserts. Final review workflow ran at ship time. Key techniques archived in graphics-build/: assemble.py, finish.py, leak/, captions generator. Remaining possible polish: Seedance b-roll finals, licensed SFX pack, style-file close-out absorption.

JOB 1 STATUS 2026-08-09 evening: base cut v4 (96.0s, 13 segments) rendered, machine-verified (render re-transcription matches script line-for-line; 12 seams frame-checked; two 1-frame freezes fixed; seg5 uses take 1 — last take defective, "92% positive" sub-audible), preview sent to user, AWAITING CUT APPROVAL. Next: checkpoint 3 = graphics plan (WhiteOak table takeover is centerpiece; table image + script in assets/). Cut history: v1-v2 scrambled by hallucinated WhisperX timeline (see [[lesson-whisperx-hallucinated-timeline]]); v3 clipped quiet take-2. transcript/chunks.json is the trusted word-time map; transcript/cutsheet.json v4 is authoritative.

JOB 1 STARTED 2026-08-09: projects/sip-horizon/ — script "SIP horizon" (Google Doc 1-t1O21uCTbWSl-7o7Q76xPIEefi8OkANdqfvZjMFJqg, shared by jasna.k@groww.in; Hinglish, WhiteOak Capital SIP-horizon study; script docx + table image saved in assets/; all script numbers verified against the table). Footage C7117.MP4 (1.95GB, Drive id 1enoWu6DfXN9sCssacPKfdBGQcTSfqpJC) NOT downloaded yet — file is private; user asked to enable "Anyone with link" sharing. Kie.ai key live in .env (1080 credits verified; user answered decisions: captions ON, brand.md diff approved, Nunito Sans download approved, Groww-official compliance). WhisperX 3.8.6 at ~/.venvs/whisperx/bin/whisperx. gdown installed (old version: no --fuzzy, pass bare file ID). Brand.md diff + Nunito download still TO APPLY. Still owed by user: footage sharing, logo/badge files, SEBI/RA text, music decision, intake grill answers.

Historical (2026-08-08) setup notes:

- **Blocker:** the first clip ("WhatsApp Video 2026-08-08 at 7.29.43 PM.mp4", ~118s, 1080x1920, creator in Groww "SIP-ing is caring" tee) has **no audio stream** (nb_streams=1). User was asked to re-send with sound (WhatsApp Document / Drive / AirDrop).
- Fonts: RESOLVED 2026-08-08 — user's brand zip installed, 40 files in assets/fonts/ (full Inter Tight family + Ivy Presto Display/Headline/Text, Ivy Journal, Ivy Ora).
- Colours: "Groww Brand Guidelines (1).pdf" (user-confirmed authoritative, 2026-08-08) VALIDATES brand.md exactly: primary #00D09C + #5367FC; accents #FCB31C (yellow — likely the caption-highlight colour), #F26B55, #67F9C8; neutrals #44475B, #B1B4B7, #CCCFD1, #ECEDEE, #F9FAFA, #FFFFFF. New vs brand.md: yellow #FCB31C and neutrals #CCCFD1/#FFFFFF unrecorded. Typography per that PDF: primary typeface Nunito Sans (marketing/social), logo font Gotham Medium — conflicts with user's stated Ivy Presto + Inter Tight; open question whether channel captions are actually Nunito Sans. The earlier "Groww Colours _Illustrations" PDF (from the fonts zip: #50DDB6, #5762FC, #F35F5F, #F7CC69, #1E2A7D, sky/sea blue) is the ILLUSTRATIONS palette only, not core brand; its dark swatches D-2/D-3 have wrong printed hex labels.
- Caption-script question ANSWERED by reference analysis 2026-08-08: channel = Hindi VO + 100% English/Roman on-screen text; captions are English TRANSLATIONS of the VO (translation pass needed after WhisperX), karaoke word-by-word in amber #F8CC63 on black chips. Captions are a per-video switch (2 of 4 refs have none).
- References analyzed: 4 channel shorts, 6-agent workflow, verified. Result: NEW style `styles/groww-shorts/` (style.md + style.json) written 2026-08-08 — the user's declared framework. `editorial` left intact. 28 verified conventions; key: 25fps, thumbnail-flash frame 1, persistent corner branding, $paper #EDEDED grid canvas, $indigo #4A69FF workhorse, green identity-only, amber emphasis, blurred slam entrances + dead-static holds + cheap exits, no count-ups, no crossfades, cold endings, no PiP (face-cards instead), Source: lines mandatory, SEBI/RA compliance furniture.
- OPEN (user decisions pending): captions default on/off; permission to download free Nunito Sans (caption font, matches official guidelines' typeface); real logo/badge assets for assets/logos/; whose SEBI/RA compliance text goes on their videos; adopt subscribe micro-skit?; brand.md diff (hook/branding rule, caption highlight amber-not-green, grouping 3-6, four type roles) proposed but NOT applied — awaiting approval.
- Not yet installed (Claude installs when first job starts): whisperx, yt-dlp. Present: node v26, ffmpeg 8.1.2, python 3.13, KIE_API_KEY in .env.

See [[user-groww-creator]] for how the user communicates.

---

## invesco-vs-motilal — 2026-08-18 rebuild (session 1098f93b)

**Source truth:** the creator supplies a CLEANED, ALREADY-CUT video with her
background composited ("with background.mp4", Drive, 4K/25fps/813.88s/20,347
frames). NO cutting is done on this job — stage 1 is transcript-only. Its audio
is identical (r=1.0000 across the whole timeline) to the green-screen
basecut-source.mp4, i.e. same performance, two exports. Working plate =
plate-480.mp4 (854x480).

**Timing authority:** our own WhisperX word map, never the sheet's timecodes
(creator directive, repeated three times). Master map = main large-v3 `hi` pass
+ sensitive gap re-pass + prior-map fill → 2,211 words, uncovered speech cut
from 47.1s to 1.7s. Beats anchored by Needleman-Wunsch over phonetically folded
tokens (Devanagari transliterated to Latin); the sheet's own word pins land
exactly, which is the check that it worked.

**Structure the source does NOT contain:** the 5s opening disclaimer card and
the ~36s closing legal scroll + endscreen. Both are prepended/appended from the
creator's real assets (assets/longform/). Silent cards are the one place the
sheet's DURATIONS are used — there is no dialogue to anchor them to.

**Document set (312-page edition):** §1 frame types, §2 conventions/seats/
palette, **§3 "Everything the script put on screen" = 23 data elements as REAL
TABLES**, §4 the per-beat spine (314 beats), §5 conformance, §6 assets.
A companion "Source Assets from the Script" PDF holds 18 screenshots + 11
tables as images — extracted to source-assets/img-*.png.

**Status:** tables rebuilt from §3 and blink fixed (39 continuous runs, not 175
mounts). Audio = voice only; no music, no SFX (SFX_ENABLED=False switch kept in
assemble.py).

**Still open:** (1) the 18 real screenshots are extracted and a SourceSlide
component exists but they are NOT wired to their beats — biggest remaining gap
to a true frame match; (2) beats 298/301 are AI substitutes for licensed stock
and show illegible AI gibberish text on the screens they depict; (3) banner
holds at beats 191/207 sit over her raised hand, violating §2.4's "held only
while her hands are down" — reported, not silently moved, per the conformance
rule; (4) the formal `watch` review has never completed end to end.

## Job: one-stock-underperformance-equity-funds (started 2026-08-22)

Groww Hinglish short, "Can just 1 stock underperformance affect equity funds?". First job driven
entirely by **comments on a script doc** rather than a brief — 17 comment-anchored instructions, 11
linked spec docs, and three levels of nested references (two YouTube reels + a 47MB Drive .mov).
That workflow is now the `instruction-harvest` skill.

- Creator supplied a FINISHED cut (`Sequence 01_1.mp4`, 1080x1920, 25fps, 97.16s, one continuous
  take, no internal cuts) — so no cutting stage; every cut/transition in the video was created here.
- Engine: **Remotion + Three.js** (user asked for three.js; also said HyperFrames was allowed but
  Remotion was already delivering). Project on a case-sensitive APFS image on the SSD.
- AI b-roll: **Higgsfield** (user redirected from Kie.ai mid-job). Seedance 2.0, 1080p, std mode.
- Everything ran on the external SSD — the internal drive sat at ~3GB free all job.
- Open with the creator at time of writing: the SEBI/RA compliance text (not in any source we hold,
  no registration number invented) and four I.T. scorecard figures carried from her sample clip while
  the card credits CNBC TV18.

Absorbed from this job per [[feedback-lessons-into-skills]]: the WhisperX dropout gate, the measured
light-leak and dust specs, caption collision solving, the Remotion/Three.js contract, Higgsfield CLI
specifics, and the blind every-frame review harness (tools now live in the finishing-pass skill).
